CREATE TABLE user (
    userID int, 
    LastName varchar(255),
    FirstName varchar(255),
    Address varchar(255),
    City varchar(255)
);
CREATE TABLE email (
    userID int, 
    email varchar(255)
);
CREATE TABLE password (
   password varchar(255)
);
